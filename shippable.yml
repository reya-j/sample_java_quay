language: java

jdk:
   - openjdk7
  # - oraclejdk7
   

build:
 # pre_ci:
  #  - docker build -t=quay.io/revathird/testjavaimg .

  pre_ci_boot:
    image_name: quay.io/revathird/testjavaimg
    image_tag: latest
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=quay.io/revathird/hitchcock3_sample_java .
    #- docker build -t=quay.io/revathird/testjavaimg .
    - docker push quay.io/revathird/hitchcock3_sample_java

integrations:
  hub:
    - integrationName: ship-quay-1
      type: quay.io
    #  branches:
     #   only:
      #    - master
      
      
      
